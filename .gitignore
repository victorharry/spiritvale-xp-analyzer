.venv/
__pycache__/
*.pyc
config.json
debug/
*.png
build/
dist/
# The .spec is build CONFIGURATION, not a build artifact: uac_admin, the
# excluded modules and the customtkinter collection all live there. Ignoring
# it made Build.bat depend on a file nobody else would have.
*.spec
!XP Analyzer.spec
installer/
*.zip

# measurements taken while validating the table: local data, and the table in
# the repo is what matters
amostras-xp.tsv
faixas-xp.json

# releases are attached to GitHub Releases, never committed
instalador/
*.exe
